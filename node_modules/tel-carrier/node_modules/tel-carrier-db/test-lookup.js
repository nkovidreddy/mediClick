'use strict';

var telDb = require('./tel-carrier-db')
  , info
  ;

/*
info = telDb.lookup(1, 801, 360, 5555);
console.log(info);

info = telDb.lookup(1, 385, 722, 5555);
console.log(info);

info = telDb.lookup(1, 757, 880, 5555);
console.log(info);
*/

info = telDb.lookup(1, 801, 850, 5555);
console.log(info);
